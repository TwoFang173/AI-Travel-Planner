# AI Travel Planner

A Python script that uses the OpenAI API to generate a structured travel plan and recommendations based on key input information such as destination and duration.

## How to Run

1.  Clone the repository.
2.  Install dependencies: `pip install openai`
3.  Add your OpenAI API key in the script.
4.  Run the script: `python travel_planner.py`
